import { LoopByNumbersPipe } from './loop-by-numbers.pipe';

describe('LoopByNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new LoopByNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
